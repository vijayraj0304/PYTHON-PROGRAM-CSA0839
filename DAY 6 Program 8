(a,b)=map(int,input("Enter the starting and the ending number :").split())
composite=[]
for i in range(a+1,b):
    for p in range(2, i):
        if (i % p)==0:
            composite.append(i)
            break
print(composite)
